# requirements.txt
# ===================================================
# اعتماديات نظام تحليل وتنبؤ الطلب — الحد الأدنى المطلوب
#
# هذا الملف يعبّر عن النوايا (ما يحتاجه المشروع).
# للتطوير والنشر استخدم requirements.lock.txt — فيه الإصدارات
# الدقيقة المُختبَرة، فتحصل على بيئة مطابقة تماماً:
#
#     pip install -r requirements.lock.txt
#
# لتحديث القفل بعد ترقية مقصودة: pip freeze > requirements.lock.txt
# ===================================================

streamlit>=1.28.0
pandas>=2.0.0
numpy>=1.24.0
plotly>=5.14.0
scipy>=1.10.0
statsmodels>=0.14.0
openpyxl>=3.1.0
pytest>=7.4.0
joblib>=1.3.0          # cache نتائج التنبؤ (services/forecast_engine/cache.py)

# ========== نماذج التنبؤ (Phase 3) ==========
prophet>=1.1.0         # يجرّ cmdstanpy + matplotlib — الاعتماد الأثقل هنا
xgboost>=2.0.0         # يجرّ nvidia-nccl-cu12 (~200MB) حتى على أجهزة بلا GPU
scikit-learn>=1.3.0    # RandomForest